# Copy to .env for local dev. Only VITE_* vars are exposed to the browser.

VITE_API_URL=http://localhost:8000

# Optional: Supabase client (public anon key only — never put service role here)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
